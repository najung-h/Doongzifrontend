version: '3.8'

services:
  webserver:
    build:
      context: .
      args:
        # 🔥 Vite 빌드 시점에 환경변수 주입
        VITE_N8N_CHATBOT_WEBHOOK_URL: /api/chatbot
        VITE_N8N_SCAN_WEBHOOK_URL: /api/scan
        VITE_N8N_CHECKLIST_WEBHOOK_URL: /api/checklist
        VITE_N8N_LEGAL_WEBHOOK_URL: ""
    container_name: doongzi-app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Nginx 설정 파일을 마운트합니다.
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      # 사용자가 직접 발급받은 SSL 인증서 파일을 마운트합니다 (프로덕션용).
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/lib/letsencrypt:/var/lib/letsencrypt:ro
    restart: unless-stopped
    environment:
      - NODE_ENV=production